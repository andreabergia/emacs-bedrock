This is my personal emacs config. It is not shared with anyone else in the world.

Don't hand-edit the `package-selected-packages` list in `custom-set-variables` (init.el). Emacs/package.el maintains it automatically when packages are installed on launch.
